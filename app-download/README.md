# App Download

This folder is for end users.

## What to download

- download the whole `MTG-Score-Tracker/` folder
- keep `MTGScoreTracker.exe`, `_internal/`, and `data/` together
- run `MTGScoreTracker.exe`

## Important

Do **not** copy only the `.exe` file. The application needs the accompanying files and folders to work correctly.
